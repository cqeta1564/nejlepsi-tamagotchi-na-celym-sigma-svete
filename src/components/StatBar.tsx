import type { PetStatKey } from '../types'

type StatBarProps = {
  label: string
  statKey: PetStatKey
  value: number
}

function StatBar(props: StatBarProps) {
  void props
  return null
}

export default StatBar
